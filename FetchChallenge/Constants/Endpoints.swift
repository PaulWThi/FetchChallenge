enum Endpoint {
    static let base = "https://d3jbb8n5wk0qxi.cloudfront.net"

    static let recipesSuccess = "\(base)/recipes.json"
    static let recipesMalformed = "\(base)/recipes-malformed.json"
    static let recipesEmpty = "\(base)/recipes-empty.json"
}
